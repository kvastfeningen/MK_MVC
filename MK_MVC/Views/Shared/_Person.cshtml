@*@model MK_MVC.ViewModels.PeopleViewModel*@
@using MK_MVC.Models
   
<table>
  <div class="row">
        
      <div class="col">
            <a asp-action="Remove" asp-route-id="@Model.PersonId">Remove  </a>
            &nbsp;&nbsp;&nbsp;
            
  </div>
  
      
  @*  <div class="col">
      @Model.PersonId
    </div>
  *@
    <div class="col">
      @Model.Name
    </div>
     @*
        <div>
            @foreach (var item in Model)
            {
               string  l = "";
        
                        @foreach (var pers in item.PersonLanguages)
                        {
                            l = l + pers.LanguageId + " ";
                        }
                        @l
            
            }
   
    </div>
   *@    
    <div class="col">
      @Model.Phone
    </div>
    <div class="col">
      @Model.City.CityName
      
    </div>
  </div>
</table>  