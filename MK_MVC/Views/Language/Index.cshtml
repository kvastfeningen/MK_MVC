@*@model MK_MVC.ViewModels.PeopleViewModel;
using MK_MVC.Models
@model IEnumerable<ShowLanguageViewModel>*@
@model IEnumerable<Language>

@{
    string l = "";
    ViewData["Title"] = "Index";
}

<a asp-action="Create">Add Language</a>
<br />
<br />
<br />
<table class="table">
    <thead>
        <tr>
            <!--<th>City Id</th>-->
            <th>Language</th>
            
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
           
            <tr>
                <td>@item.LanguageName</td>


                <td><a asp-action="ShowPeopleLanguages" asp-route-id="@item.LanguageId">People</a>  </td>
            </tr>
        }
    </tbody>
</table>