@*@model MK_MVC.ViewModels.PeopleViewModel;
using MK_MVC.Models*@
@model IEnumerable<Language>

@{
    string personer = "";
    ViewData["Title"] = "Index";
}

<a asp-action="Create">Add Language</a>
<br />
<br />
<br />
<table class="table">
    <thead>
        <tr>
            <!--<th>City Id</th>-->
            <th>Language</th>
            <th>People</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            personer = "";
            <tr>
                <td>@item.LanguageName</td>
                <td>
                    @foreach (var pers in item.PersonLanguages)
                    {
                        personer = personer + pers.PersonId + ",  ";
                    }
                    @personer
                </td>
                
                <td>  </td>
            </tr>
        }
    </tbody>
</table>